defmodule BambooWeb.HostsController do
  use BambooWeb, :controller

  def index(conn, _params) do
    # TODO
    render(conn, :index)
  end
  def edit(conn, _params) do
    # TODO
    render(conn, :edit)
  end
  def update(conn, _params) do
    # TODO
    render(conn, :index)
  end
end
