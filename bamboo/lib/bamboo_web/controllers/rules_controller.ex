defmodule BambooWeb.RulesController do
  use BambooWeb, :controller

  def index(conn, _params) do
    # TODO
    render(conn, :index)
  end
  def new(conn, _params) do
    # TODO
    render(conn, :new)
  end
  def create(conn, _params) do
    # TODO
    render(conn, :index)
  end
  def edit(conn, _params) do
    # TODO
    render(conn, :edit)
  end
  def update(conn, _params) do
    # TODO
    render(conn, :index)
  end
  def destroy(conn, _params) do
    # TODO
    render(conn, :index)
  end
end
