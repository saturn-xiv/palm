import type { IInputRule } from "../../api/rules";

interface IProps {
  devices: string[];
  item?: IInputRule;
}
const Widget = ({ devices }: IProps) => {
  return <></>;
};

export default Widget;
