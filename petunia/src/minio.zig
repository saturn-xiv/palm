const std = @import("std");

const Minio = struct {
    fn sign(_: *Minio) void {
        // TODO
    }
    fn verify(_: *Minio) void {
        // TODO
    }
};

test "minio" {
    try std.testing.expect(1 + 1 == 2);
}
