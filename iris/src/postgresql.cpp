#include "iris/database.hpp"
#include "iris/utils.hpp"

#include <spdlog/spdlog.h>

void iris::PostgreSql::dump(const std::filesystem::path& file) const {
  // TODO
}
void iris::PostgreSql::restore(const std::filesystem::path& file) const {
  // TODO
}
