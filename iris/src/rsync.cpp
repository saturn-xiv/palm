#include "iris/filesystem.hpp"
#include "iris/utils.hpp"

#include <spdlog/spdlog.h>

void iris::Filesystem::dump(const std::filesystem::path& file) const {
  // TODO
}
void iris::Filesystem::restore(const std::filesystem::path& file) const {
  // TODO
}
