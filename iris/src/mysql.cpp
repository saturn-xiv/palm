#include "iris/database.hpp"
#include "iris/utils.hpp"

#include <spdlog/spdlog.h>

void iris::MySql::dump(const std::filesystem::path& file) const {
  // TODO
}
void iris::MySql::restore(const std::filesystem::path& file) const {
  // TODO
}
