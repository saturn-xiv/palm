[Unit]
Description=gRPC service of palm
After=network.target

[Service]
Type=simple
User=www-data
Group=www-data
WorkingDirectory=/var/lib/palm
ExecStart=/usr/bin/palm -c /etc/palm/config.toml --rpc
Restart=on-failure

[Install]
WantedBy=multi-user.target
